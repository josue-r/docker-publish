{
    "deployUrl": "{{APP_HOST}}/{{APP_VERSION}}/",
    "baseHref": "/",
    "env": {
        "production": "${PRODUCTION}",
        "environmentName": "${ENVIRONMENT_NAME}",
        "gateway": "${GATEWAY}",
        "deployUrl": "{{APP_HOST}}/{{APP_VERSION}}/",
        "version": "{{APP_VERSION}}"
    }
}
