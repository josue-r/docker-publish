/* A result object for the nav-search component. */
export interface NavSearchOption {
    /* The display name. */
    name: string;
    /* The router path. */
    path: string;
    /* The index of the match. */
    index: number;
}
