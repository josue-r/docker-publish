/** Various log levels that can be configured in LogLevelProvider. */
export enum LogLevel {
    DEFAULT = Number.MIN_SAFE_INTEGER,
    ALL = 0,
    DEBUG = 1,
    INFO = 2,
    WARN = 3,
    ERROR = 4,
    OFF = 5,
}
