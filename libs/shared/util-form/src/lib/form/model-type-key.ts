/**
 * This key is added to the FormFactoryOptions to track what the source type is backing the form.  As of initial development,
 * this is just use for supportability though this may change in future versions.
 */
export const modelTypeKey = '_modelType';
