export namespace DividerConstants {
    export enum Orientation {
        Vertical = 'vertical',
        Horizontal = 'horizontal',
    }
    export enum Color {
        Aluminum = 'aluminum',
        Red = 'red',
    }

    export enum Thickness {
        Thin = 'thin',
        Thick = 'thick',
    }
}
