/**
 * Mapping of user information to be used for profile purposes.
 */
export interface User {
    id: string;
    name: string;
    firstName: string;
    lastName: string;
    email: string;
    realm: string;
}
